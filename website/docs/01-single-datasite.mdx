# 1. Single-Datasite Apps

In this tutorial we'll build an app that runs on a single datasite: the ***CPU Tracker Member*** app.

The app is pretty simple, it gathers CPU data from your local machine and dumps it in a file in your datasite.

## File structure
